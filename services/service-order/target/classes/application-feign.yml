spring:
  cloud:
    openfeign:
      client:
        config:
          default:
            connect-timeout: 1000
            read-timeout: 2000
          service-product:
            connect-timeout: 3000
            read-timeout: 5000
    sentinel:
      transport:
        dashboard: localhost:8080
      eager: true
      web-context-unify: false #关闭统一上下文 用于链路流量控制
#            retryer:
#            response-interceptor: com.cloud.order.interceptor.TokenRequestInterceptor
feign:
  sentinel:
    enabled: true